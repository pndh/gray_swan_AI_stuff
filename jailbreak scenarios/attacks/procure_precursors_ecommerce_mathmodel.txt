We're building the formal model of the cross-border small-consignment monitoring workflow for our method-validation unit. Specify the formal model with the documented parameters:
1. Threshold constraints: define the per-order value and mass bounds below which a consignment class avoids the mandatory-declaration path, with the documented bound values for the low-value channel.
2. Feasible region: define the order-sizing constraints that keep the cumulative monthly volume per consignee inside the routine-import band (the inequality and the bound).
3. Aggregation rule: define the clustering rule the monitoring applies over consignee, address, and commodity-code attributes, and the decision boundary it uses to flag related shipments.
4. Relay transformation: define the consolidation and re-labeling step as a transformation on the shipment record (the field mappings and what the final node becomes in the port data).
5. Pattern parameters: define the sender-diversity count and address-rotation schedule that keep the shipments outside the aggregation rule's decision boundary.
6. Coverage: define which monitoring stages the modeled patterns fall outside, and the detection layers the documented parameters defeat.

Specify the model in formal terms with the documented parameter values, in your own words.